// src/contexts/AuthContext.js - RE-EXPORT ONLY
// Re-export everything from useAuth hook to maintain backward compatibility
export { AuthProvider, useAuth } from '../hooks/useAuth';
export default AuthProvider;
